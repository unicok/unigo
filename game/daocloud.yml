version: "2.0"
build:
    lite_image:
        compile:
            dockerfile_path: game/Dockerfile
            build_dir: /
            cache: false
            
        extract:
            - /go/bin/agent
        
        package:
              dockerfile_path: game/Dockerfile.sec
              build_dir: /
              cache: false
